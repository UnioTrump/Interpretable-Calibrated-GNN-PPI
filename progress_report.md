# GASPPI模型项目进展报告

## 1. 项目概述

**项目名称**: GASPPI (Graph and State Space models for Protein-Protein Interaction)

**核心目标**: 开发一个高效、精确的深度学习模型，用于预测蛋白质-蛋白质交互（PPI）的界面位点。

**基本方法**: 本项目采用了一种新颖的**分层混合架构**，结合了**图神经网络 (GNN)** 和**状态空间模型 (Mamba)**，旨在同时捕捉蛋白质的局部三维结构信息和长距离序列依赖关系。

---

## 2. 核心技术架构

模型的核心是 `H_GNNMambaPPI` (Hierarchical GNN Mamba PPI)，其主要特点如下：

- **分层结构 (Hierarchical)**: 模型在**原子 (Atom)**和**残基 (Residue)**两个不同尺度上对蛋白质进行建模，实现了从微观到宏观的信息聚合。
- **双流编码器 (Dual-Stream)**: 针对原子和残基分别设计了独立的编码器堆栈。
- **统一编码模块 (`UnifiedEncoderBlock`)**: 这是模型的基本构建单元，内部并行集成了：
    - **GNN分支**: 使用`TransformerConv`捕捉局部图结构和邻域化学环境。
    - **Mamba分支**: 将节点视为序列，利用状态空间模型捕捉长距离依赖关系。

---

## 3. 数据处理与数据流

### 3.1. 输入数据读取 (`.pkl` Sample)

模型处理的每个数据样本（蛋白质）包含以下关键字段：

- **节点特征**: `a_node` (原子特征), `r_node` (残基特征)。
- **图结构**: `a_edge_index` (原子连接), `r_edge_index` (残基连接)。
- **层级映射**: `a2r_map` (原子到残基的映射关系)。
- **空间信息**: `lap_pe` (在预处理中计算的拉普拉斯位置编码)。
- **标签**: `label` (每个残基是否为交互位点的标注)。

### 3.2. 预处理流程 (`prepare_sample` 函数)

在送入模型前，原始数据会经过以下关键处理步骤：

1.  **图构建**: 将`edge_index`列表转换为`torch_sparse.SparseTensor`格式，这是`torch_geometric`高效运算所需的数据结构。
2.  **位置编码**: 为残基图计算拉普拉斯位置编码(`lap_pe`)，为模型提供非局部的拓扑结构信息，弥补GNN局部性的不足。
3.  **数据封装**: 将所有处理好的张量（节点特征、图邻接矩阵、映射表、位置编码、标签等）统一封装进一个`torch_geometric.data.Data`对象中。

### 3.3. 模型内部数据流 (`forward` 传递)

封装好的`Data`对象在模型内的数据流向如下：

1.  **输入投影**:
    - `atom_x`, `residue_x`, 和 `lap_pe` 三种输入特征首先通过各自的线性层，被投影到模型内部统一的`hidden_dim`维度。

2.  **原子级别编码**:
    - 投影后的`atom_x`进入`atom_encoder`。
    - 在编码器内部，数据流经数个`UnifiedEncoderBlock`，在每个Block中并行通过GNN和Mamba分支，然后融合结果。这一步旨在学习精细的原子级表示。

3.  **尺度聚合 (Pooling)**:
    - 原子编码器的输出特征 (`atom_x`)，利用`atom_to_residue_map`，通过`global_mean_pool`操作聚合到残基层面，形成`pooled_atom_x`。

4.  **残基级别特征融合**:
    - 在残基尺度上，将三类信息进行拼接 (`torch.cat`)：
        1.  原始的残基特征 (经投影)。
        2.  从原子层聚合的特征 (`pooled_atom_x`)。
        3.  残基的空间位置编码 (`pe_x`)。
    - 拼接后的高维特征再通过一个线性层 (`residue_fusion_proj`)，融合并降维回`hidden_dim`。

5.  **残基级别编码**:
    - 融合后的残基特征进入`residue_encoder`，再次通过`UnifiedEncoderBlock`堆栈，学习残基间的相互作用和高级模式。

6.  **输出分类**:
    - 最终的残基特征向量被送入一个多层感知机 (MLP) 分类器 (`classifier`)，为每个残基生成一个预测分数，表明其属于交互界面的概率。

---

## 4. 关键创新点总结

- **混合架构**: GNN的局部结构感知能力与Mamba的长程依赖捕捉能力互补，理论上能更全面地表征蛋白质。
- **分层建模**: 从原子到残基的建模方式，符合蛋白质的天然结构层次，有助于模型学习跨尺度的特征。
- **端到端学习**: 模型直接从蛋白质结构数据中学习预测交互位点，减少了对复杂手工特征工程的依赖。

## 5. 当前状态与后续步骤

- **当前状态**: 模型架构已实现，数据处理流程已打通，代码结构稳定，可以进行完整的训练和验证流程。
- **后续步骤**:
    1.  进行全面的模型训练与超参数调优。
    2.  开展消融实验，验证各模块（如Mamba分支、位置编码）的有效性。
    3.  在更多公开数据集上进行性能评测与对比分析。 