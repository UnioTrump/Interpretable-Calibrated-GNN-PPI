# GASPPI模型项目进展报告

## 1. 项目概述

**项目名称**: GASPPI (Graph and State Space models for Protein-Protein Interaction)

**核心目标**: 开发一个高效、精确的深度学习模型，用于预测蛋白质-蛋白质交互（PPI）的界面位点。

**核心架构**: 本项目采用了一种新颖的**分层双流架构**。在原子级别，模型学习精细的物理化学环境；在残基级别，模型通过并行的**特征流**和**几何流**，分别处理蛋白质的生化特性和三维拓扑结构，最终融合两者信息进行预测。

---

## 2. 核心技术架构：分层与双流

模型的核心 `H_GNNMambaPPI` 体现了“分层”和“双流”两大设计思想。

- **分层 (Hierarchical)**: 模型首先在**原子尺度**上进行编码，然后将信息聚合到**残基尺度**，符合蛋白质的天然结构层次。
- **双流 (Dual-Stream)**: 在信息最密集的残基尺度，模型将信息处理一分为二，形成两条并行的、目标明确的编码路径。

### 2.1. 构建块: `InteractionBlock`
这是模型的基本处理单元，它是一个可配置的、包含GNN和/或Mamba的残差块，负责在特定尺度上学习节点间的复杂交互。

### 2.2. 残基层的双流设计 (Dual-Stream Design)

这是本次架构升级的核心。进入残基尺度的信息被分为两类，并送入专属的编码器：

1.  **特征流 (Feature Stream)**:
    - **目标**: 理解蛋白质的**生化本质**。
    - **输入**: 由原始残基特征（如氨基酸类型、疏水性）和从原子层聚合的特征（代表局部化学环境）拼接而成。
    - **作用**: 专注于回答“**这个残基是什么，它由什么构成？**”

2.  **几何流 (Geometry Stream)**:
    - **目标**: 理解蛋白质的**空间拓扑结构**。
    - **输入**: 拉普拉斯位置编码（Laplacian Positional Encoding, PE）。
    - **作用**: 专门学习PE中蕴含的图结构信息，专注于回答“**这个残基在蛋白质结构中的哪个位置？**”

这种关注点分离的设计，使得模型能够更深入、更纯粹地学习两种完全不同但互补的信息，避免了在单一编码器中处理混合信号所带来的信息干扰。

---

## 3. 详细数据流

封装好的`Data`对象在模型内的完整数据流如下：

1.  **输入投影**:
    - 原始的`atom_x`, `residue_x`, 和 `lap_pe` 特征首先通过各自专属的**非线性投影块 (MLP)**，被映射到模型内部统一的`hidden_dim`维度。该步骤能有效处理高维输入（如`residue_x`）的信息瓶颈问题。

2.  **第一阶段：原子级别编码**:
    - 投影后的`atom_x`进入`atom_encoder`（由`InteractionBlock`构成）。
    - `atom_encoder`学习原子间的相互作用，输出蕴含精细化学环境的原子嵌入。

3.  **尺度聚合 (Pooling)**:
    - 经过编码的`atom_x`，利用`atom_to_residue_map`，通过`global_mean_pool`操作聚合到残基层面，形成`pooled_atom_x`。

4.  **第二阶段：残基级别的双流并行处理**:
    - **准备特征流输入**: 将投影后的`residue_x`和聚合后的`pooled_atom_x`进行拼接，再通过一个线性融合层(`feature_fusion_proj`)得到特征流的最终输入。
    - **准备几何流输入**: 投影后的`pe_x`直接作为几何流的输入。
    - **并行编码**:
        - 特征流输入进入`feature_encoder`，学习生化特性的高级表示。
        - 几何流输入进入`geometry_encoder`，学习空间拓扑的高级表示。
    - 两个编码器（均由`InteractionBlock`构成）同时独立地工作。

5.  **最终融合与分类**:
    - **信息融合**: 从特征流和几何流得到的两个高维嵌入（`processed_features` 和 `processed_geometry`）进行**逐元素相加**，实现高效且强大的信息融合。
    - **分类**: 融合后的最终嵌入向量被送入一个MLP分类器，为每个残基生成最终的交互预测分数。

---

## 4. 关键创新点总结

- **双流架构**: 在残基级别创新性地将特征与几何分离处理，使得模型能更深入地学习两种信息的独特模式。
- **分层建模**: 从原子到残基的建模方式，符合蛋白质的天然结构层次，有助于模型学习跨尺度的特征。
- **非线性投影**: 采用MLP对输入进行投影，有效解决了处理大型预训练模型（如ESM）输出的高维特征时可能出现的信息瓶颈问题。

## 5. 当前状态与后续步骤

- **当前状态**: 核心架构已重构完成，代码逻辑清晰，可读性强，能够支持后续的各类实验。
- **后续步骤**:
    1.  进行全面的模型训练与超参数调优。
    2.  开展消融实验，定量评估双流架构、非线性投影等设计的有效性。
    3.  在更多公开数据集上进行性能评测与对比分析。 